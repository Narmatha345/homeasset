# Base URL for the HomeAsset API. Left empty (default) so requests go through
# the Vite dev-server proxy defined in vite.config.ts (target: localhost:4000).
# Set explicitly only when serving the frontend separately from the backend.
VITE_API_URL=
